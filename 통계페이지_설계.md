# 통계 페이지 — 지표 3가지

기존 스키마(`chat_sessions`/`chat_messages`/`rag_retrieval_logs`, [LawRagTableCreate.sql](LawRagTableCreate.sql))만으로
계산 가능한 지표 3가지와 산출 방법을 정리한다. 스키마 변경 없음.

## 1. 응답률 (간접 계산)

`chat_messages`엔 성공/실패를 표시하는 컬럼이 없다. 대신 질의가 실패(타임아웃 등)하면
USER 메시지는 저장되지만 그 뒤 BOT 메시지는 저장되지 않는 흐름([QueryController.cs](main-server/Controllers/QueryController.cs))을
이용해, **USER 메시지 수 대비 BOT 메시지 수**로 간접 계산한다.

```sql
SELECT
    SUM(sender_type = 'USER') AS total_questions,
    SUM(sender_type = 'BOT') AS answered_questions,
    ROUND(SUM(sender_type = 'BOT') / SUM(sender_type = 'USER') * 100, 1) AS response_rate_pct
FROM chat_messages;
```

현재 데이터 예시: USER 31건 / BOT 21건 → 응답률 약 67.7%

**한계**: USER-BOT을 1:1로 정확히 짝짓는 게 아니라 전체 건수 비교라, 세션 하나가 중간에 끊겨도
비율에는 반영되지만 "어느 질문이 실패했는지"까지는 알 수 없다. 필요하면 나중에
`chat_messages`에 `status` 컬럼을 추가해 정확한 1:1 매칭으로 전환할 수 있다.

## 2. Rag 선택 비율

`rag_retrieval_logs.target_index`에 질의마다 어느 카테고리(precedents/statutes/adjudications/interpretations)로
검색됐는지 그대로 남아있어 직접 집계 가능하다.

```sql
SELECT
    target_index,
    COUNT(*) AS count,
    ROUND(COUNT(*) * 100.0 / SUM(COUNT(*)) OVER (), 1) AS percentage
FROM rag_retrieval_logs
GROUP BY target_index
ORDER BY count DESC;
```

현재 데이터 예시: precedents 22건(71.0%) / statutes 5건(16.1%) / interpretations 4건(12.9%) / adjudications 0건

## 3. 누적 상담 세션 수

"누적 인원"은 `chat_sessions.user_id`가 항상 NULL이라([DbServerClient.cs](main-server/Clients/DbServerClient.cs)
로그인/사용자 식별 체계가 없음) 계산할 수 없다. 대신 **누적 세션(상담) 건수**로 대체한다.
UI에는 "누적 이용자"가 아니라 "누적 상담 건수"로 명확히 표기해, 한 사람이 여러 번 접속해도
여러 건으로 잡힌다는 걸 착각하지 않도록 한다.

```sql
-- 전체 누적 건수
SELECT COUNT(*) AS total_sessions FROM chat_sessions;

-- 날짜별 추이 + 누적치 (그래프용)
SELECT
    DATE(created_at) AS date,
    COUNT(*) AS daily_sessions,
    SUM(COUNT(*)) OVER (ORDER BY DATE(created_at)) AS cumulative_sessions
FROM chat_sessions
GROUP BY DATE(created_at)
ORDER BY date;
```

현재 데이터 예시: 누적 28건
