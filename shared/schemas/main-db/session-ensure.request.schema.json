{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "main-db/session-ensure.request.schema.json",
  "title": "main-db session-ensure request",
  "description": "MainServer -> DBServer: chat_sessions row가 없으면 생성 (있으면 아무 것도 안 함)",
  "type": "object",
  "properties": {
    "session_id": { "type": "string" },
    "user_id": { "type": ["string", "null"] },
    "title": { "type": ["string", "null"] }
  },
  "required": ["session_id"]
}
